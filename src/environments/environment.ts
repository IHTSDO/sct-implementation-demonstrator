export const environment = {
  production: false,
  country: 'default',

  allergyList: {
    enableNotes: false,
    enablePropensity: true,
    enablePatient: false,
    enableExposureRoute: true
  }
};