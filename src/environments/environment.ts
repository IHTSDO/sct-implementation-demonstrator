export const environment = {
  production: false,
  country: 'default',
  defaultFhirServerIndex: 1,

  allergyList: {
    enableNotes: false,
    enablePropensity: true,
    enablePatient: false,
    enableExposureRoute: true
  }
};